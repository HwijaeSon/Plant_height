{
  "experiment": "four_genotypes_20260915",
  "data_protocol": "four_genotypes_20260915",
  "model": "phytoode_prefix_parameters_no_light",
  "parameter_head_inputs": "genotype embedding (4), observed scaled prefix lengths (4), prefix presence masks (4)",
  "parameter_head_architecture": [
    12,
    8,
    2
  ],
  "initialization": "Preserve genotype weights and all core initialization; new prefix/mask columns start at zero.",
  "lambda_k": 0.0,
  "lambda_ode_grid": [
    100.0
  ],
  "seeds": [
    1,
    2,
    3
  ],
  "search_drop_fraction": 0.0,
  "evaluation_drop_fractions": [
    0.0,
    0.25,
    0.5
  ],
  "mask_seed_rule": "20260910 + training seed; masks regenerated for the approved cohort",
  "candidate_selection": "Fixed manuscript coefficient; no coefficient selection in this rerun.",
  "missingness_protocol": "Retrain independently on each shared observation mask; all six models use the four approved genotypes.",
  "test_policy": "Train all candidates and transfer runs with train/validation only. Freeze both coefficient variants and all unique checkpoint hashes before test scoring.",
  "common": {
    "epochs": 1500,
    "lr": 0.01,
    "weight_decay": 0.0001,
    "eval_every": 20,
    "min_epoch": 200,
    "val_smooth": 3,
    "gradient_clip": 1.0
  },
  "scope": "Fixed-setting retraining on the approved cohort; no original checkpoints reused.",
  "illumination_input": false,
  "fixed_lambda_reference": 100.0,
  "fixed_reference_rationale": "Retain the manuscript setting while changing only the allowed cohort and corresponding genotype dimension."
}
