{
  "experiment": "expanded_no_light_lambda_20260911",
  "data_protocol": "prefix_forecast_20260911",
  "model": "phytoode_prefix_parameters_no_light",
  "parameter_head_inputs": "genotype embedding (4), observed scaled prefix lengths (4), prefix presence masks (4)",
  "parameter_head_architecture": [
    12,
    8,
    2
  ],
  "initialization": "Preserve genotype weights and all core initialization; new prefix/mask columns start at zero.",
  "lambda_k": 0.0,
  "lambda_ode_grid": [
    0.001,
    0.003,
    0.01,
    0.03,
    0.05,
    0.1,
    0.2,
    0.3,
    0.5,
    1.0,
    5.0,
    10.0,
    50.0,
    75.0,
    100.0,
    150.0,
    200.0,
    300.0,
    500.0,
    1000.0,
    3000.0,
    10000.0
  ],
  "seeds": [
    1,
    2,
    3
  ],
  "search_drop_fraction": 0.0,
  "evaluation_drop_fractions": [
    0.0,
    0.25,
    0.5
  ],
  "mask_seed_rule": "20260910 + training seed; unchanged paired masks from the preceding benchmark",
  "candidate_selection": "Lowest mean actual 48-h validation RMSE over all three seeds under natural missingness; ties favor smaller lambda_ode.",
  "missingness_protocol": "Transfer the validation-selected coefficient unchanged to 25% and 50% additional removal. Reuse the prior final evaluations if coefficient 100 is selected again.",
  "test_policy": "This follow-up uses a previously analyzed dataset. Select among all 22 coefficients using validation only; freeze all nine selected checkpoint hashes before any new test scoring. Prior coefficient-100 test scores are reused only if selected again.",
  "common": {
    "epochs": 1500,
    "lr": 0.01,
    "weight_decay": 0.0001,
    "eval_every": 20,
    "min_epoch": 200,
    "val_smooth": 3,
    "gradient_clip": 1.0
  },
  "scope": "Expand only lambda_ODE for the unchanged no-light prefix-parameter PhytoODE; reuse the prior ten validation-only candidates.",
  "illumination_input": false,
  "fixed_lambda_reference": 100.0,
  "fixed_reference_rationale": "The validation-selected coefficient from the completed light-input follow-up; frozen before no-light search.",
  "added_lambda_ode_grid": [
    0.001,
    0.003,
    0.03,
    0.05,
    0.2,
    0.3,
    75.0,
    150.0,
    200.0,
    300.0,
    3000.0,
    10000.0
  ],
  "reused_lambda_ode_grid": [
    0.01,
    0.1,
    0.5,
    1.0,
    5.0,
    10.0,
    50.0,
    100.0,
    500.0,
    1000.0
  ]
}
